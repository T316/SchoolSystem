@model SchoolSystem.Models.BindingModels.DirectorPanel.Subjects.SubjectsBm

@{
    ViewBag.Title = "Добави предмет";
}

<h2 class="text-center">Добави предмет</h2>

@using (Html.BeginForm()) 
{
    
    <div class="form-horizontal">
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group">
            @Html.Label("Наименование на предмета", htmlAttributes: new { @class = "control-label col-md-4" })
            <div class="col-md-8">
                @Html.EditorFor(model => model.Name, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.Name, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <div>
                <input type="submit" value="Добави" class="btn btn-success" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Назад", "All", null, new { @class = "btn btn-sm btn-default go-back-btn" })
</div>
