@model SchoolSystem.Models.ViewModels.SchoolDiary.Subjects.SubjectDetailsVm

@{
    ViewBag.Title = "Информация за предмета";
}

<h2 class="text-center">@ViewBag.Title</h2>

<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

        @if (@Model.Teacher != null)
        {
            <dt>
                @Html.DisplayNameFor(model => model.Teacher)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Teacher.User.Name)
            </dd>
        }

    </dl>
</div>
<p>
    @Html.ActionLink("Назад", "All", new { id = Model.Grade.Id }, new { @class = "btn btn-sm btn-default go-back-btn" })
</p>
