@model IEnumerable<SchoolSystem.Models.ViewModels.SchoolDiary.Subjects.AllSubjectsFromGradeVm>

@{
    ViewBag.Title = "Subjects";
}

@if (Model.Any())
{
    <h2 class="text-center">Subjects for @Html.DisplayFor(model => Model.First().Grade.Value)@Html.DisplayFor(model => Model.First().Grade.Class) grade</h2>
}

<table class="table">

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Details", "SubjectDetails", new { id=item.Id }, new { @class = "btn btn-sm btn-primary" })
        </td>
    </tr>
}

</table>

@Html.ActionLink("Go back", "All", "Grades", null, new { @class = "btn btn-sm btn-default go-back-btn" })